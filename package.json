{
  "private": true,
  "version": "0.0.0",
  "name": "example",
  "devDependencies": {
    "@babel/core": "*",
    "@babel/preset-env": "*",
    "babel-jest": "*",
    "browserify": "^17.0.0",
    "jest": "*"
  },
  "scripts": {
    "test": "jest",
    "build": "browserify main.js -o bundle.js"
  },
  "dependencies": {
    "jest-environment-jsdom": "^29.1.2",
    "jsdom": "^20.0.0"
  }
}
